# Copy to .env in this directory. Required for agent, sessions summarizer, and ingest.
ANTHROPIC_API_KEY=sk-ant-api03-...

# Optional: extra allowed browser origins (comma-separated). Vercel *.vercel.app is already allowed via regex in main.py.
# CORS_ORIGINS=https://my-custom-domain.com
